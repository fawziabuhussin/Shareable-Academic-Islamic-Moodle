# Docs

This folder is intentionally minimal in the boilerplate edition.

Use the root `README.md` as the primary onboarding and deployment reference.
Add your own project-specific architecture notes, decision records (ADRs),
API documentation, and operational runbooks here as your implementation grows.
